import XCTest
@testable import R_D_Inventory___Server

class R_D_Inventory___ServerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        XCTAssertEqual(R_D_Inventory___Server().text, "Hello, World!")
    }


    static var allTests : [(String, (R_D_Inventory___ServerTests) -> () throws -> Void)] {
        return [
            ("testExample", testExample),
        ]
    }
}
